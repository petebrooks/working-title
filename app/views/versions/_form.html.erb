<%= form_tag project_versions_path(@version.project) do%>
  <%= text_area_tag 'version[contribution]' %>
  <%= hidden_field_tag 'version[previous_version_id]', @version.id %>
  <%= hidden_field_tag 'version[insertion_index]', -1 %>
  <%= submit_tag "Add" %>
<% end %>
