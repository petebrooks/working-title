<div class = "container">
  <div class = "all_projects">
    <h3>All Projects</h3>
    <% if @projects %>
      <ul>
        <% @projects.each do |project| %>
          <li><a href="<%= project_path(project) %>"><%= project.name %></a></li>
        <% end %>
      </ul>
    <% end %>
  </div>
</div>
